---- Server-side handler for SelectiveSpendAtoms
--local ReplicatedStorage = game:GetService("ReplicatedStorage")
--local SelectiveSpendAtomsAction = ReplicatedStorage.Inventory:WaitForChild("SelectiveSpendAtomsAction")

--SelectiveSpendAtomsAction.OnServerEvent:Connect(function(player, atomsSpent)
--    -- TODO: Apply bonus logic here
--    print(player.Name .. " spent " .. tostring(atomsSpent) .. " atoms for bonus!")
--    -- Example: Increase bonus, update stats, etc.
--end)

